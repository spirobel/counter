# counter

To install dependencies:

```bash
bun install
```

To run backend example:

```bash
bun run counter.ts
```

To run frontend example:

```bash
bun run frontend_counter.ts
```

use --hot to reload on change

use NODE_ENV=production to run in production mode (no hmr reload script + frontend minification)
